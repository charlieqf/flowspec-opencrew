# Session 21 Baseline Reference

The original OpenFlow Session `#21` was used as the artifact baseline for OpenClip validation.

Baseline reference points:

- three scheme outputs
- scheme counts: 12 / 6 / 3
- keyframes, transcripts, storyboards, reports, clips all exist
- result package is JSON-primary

OpenClip keeps the same overall deliverable shape, but changes formula slot mapping to be dynamic by selected formula.
