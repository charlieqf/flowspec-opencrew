# OpenClip

OpenClip is the standalone video analysis module under `OpenCrew`.

It owns:

- independent backend router and task model
- independent frontend module and routes
- one OpenClip Task mapped to one OpenCode Session
- prompt generation, skill generation, run and rerun in the same session context
