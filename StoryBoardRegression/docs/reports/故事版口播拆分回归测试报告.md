# 故事版口播拆分回归测试报告

生成日期：2026-05-30

## 结论

拆分后的故事版口播代码已完成 Task #31 回归。资产绑定、释放、回收、历史恢复、最终删除、上传池删除联动解绑、Dialogue 删除/合并回收、保存重排保留，以及生成视频播放分片检查均通过。

本轮测试没有把大文件复制到新的位置。测试脚本只创建极小的临时测试图片，执行后恢复 Task #31 编辑计划和资产索引，并清理测试 Working/history 文件。

## 已执行测试

| 测试 | 结果 | 说明 |
| --- | --- | --- |
| 服务端语法检查 | 通过 | `PYTHONPYCACHEPREFIX=/private/tmp/opencrew_pycache python3 -m py_compile OpenCrew/OpenClip/backend/openclip_backend/koubo_storyboard_router.py` |
| 前端生产构建 | 通过 | `npm run build`，75 modules transformed；仅保留既有 chunk size warning |
| Python StoryBoard 测试 | 通过 | `./.venv/bin/pytest tests/analysis_v1/test_storyboard.py`，1 passed |
| Fixed timing 边界测试 | 通过 | `node tests/storyboard/fixed_reorganize_timing_boundaries.mjs`，5 项 checks 全 true |
| Task #18 fixed reorganize regression | 通过 | 已修正 fixture 前置条件依赖，验证合并/新增 Dialogue 后固定重排仍保留行 |
| Task #31 资产生命周期 API 回归 | 通过 | `/private/tmp/task31_remaining_asset_cases_report.json`，28/28 passed |
| Computer Use 页面验证 | 通过 | Chrome 打开 `http://127.0.0.1:18080/#/koubo-storyboard/tasks/31`，页面正常、历史素材删除按钮可见、视频卡片可见 |
| 生成视频播放检查 | 通过 | 视频 raw 接口返回 `206 Partial Content`，`content-type: video/mp4` |

## Task #31 资产生命周期覆盖

| 案例 | 结果 |
| --- | --- |
| Task #31 detail 加载 | 通过 |
| 生成素材绑定到 Dialogue 人物图槽 | 通过 |
| 生成素材清除一次进入 history | 通过 |
| history manifest 保留完整槽位 `Image_01` | 通过 |
| history 素材重新绑定时恢复到 `Working/` | 通过 |
| 恢复后的生成素材再次一键释放 | 通过 |
| history 素材最终删除且文件不残留 | 通过 |
| 上传素材绑定不复制到 `Working/` | 通过 |
| 上传池删除同步解绑当前 Dialogue | 通过 |
| 上传池删除不进入 history | 通过 |
| 原视频抽帧素材绑定为参考图 | 通过 |
| 原视频参考图释放不删除源文件 | 通过 |
| 保存/重排不误清上传绑定 | 通过 |
| 删除 Dialogue 回收其生成素材 | 通过 |
| 合并/移除 Dialogue 回收消失 Dialogue 的生成素材 | 通过 |
| 同槽替换立即回收旧生成素材 | 通过 |
| 页面仍存在生成视频播放源 | 通过 |
| 生成视频 raw 分片播放接口 | 通过 |

## 本轮修复点

| 区域 | 修复 |
| --- | --- |
| 后端资产绑定 | Dialogue 级 `working_assets` 作为绑定真源；上传/原始素材不复制到 `Working/` |
| 后端释放回收 | 生成素材释放/替换/Dialogue 消失时进入 `assets/history`；上传素材删除不进 history |
| 后端 history 删除 | 新增 history 资产最终删除接口，一次删除即移除文件与 manifest item |
| 后端上传池删除 | 支持删除目录扫描出来的上传素材，并同步清理当前 plan 引用 |
| 保存/重排 | `save` 对比前后 plan，自动回收被删除 Dialogue 持有的生成素材 |
| 前端素材池 | history 卡片增加删除按钮，上传/历史删除共用正在删除状态 |
| 播放展示 | Dialogue 卡片读取 Dialogue 级 audio/image/video，生成视频可直接渲染为可播放 media |
| 测试脚本 | Task #18 回归不再依赖当前 fixture 必须天然存在同 Scene 相邻 Dialogue |

## 对 STORYBOARD_OUTPUT_STRUCTURE.md 的覆盖

| 测试计划区域 | 覆盖状态 |
| --- | --- |
| 资产绑定 | 已覆盖：原素材、上传素材、history、generated Working 均覆盖 |
| 资产释放 | 已覆盖：source/image/video 路径释放；生成素材释放进入 history |
| 资产回收 | 已覆盖：清除、替换、Dialogue 删除、Dialogue 合并/移除 |
| 上传池删除 | 已覆盖：删除一次即文件删除、引用清空、不进入 history |
| history 恢复 | 已覆盖：history 重新绑定恢复到 Working |
| history 最终删除 | 已覆盖：删除一次即文件和 manifest item 清除 |
| 保存与重排 | 已覆盖：保存/固定重排不误清有效上传绑定 |
| 播放生成视频 | 已覆盖：页面 video 可见，raw media 返回 206 分片 |
| 大文件复制禁令 | 已覆盖：测试不复制现有大视频；只读/绑定现有视频路径 |

## 备注

`npm run build` 仍提示 bundle 超过 500 kB，这是既有构建体积提示，不影响本轮拆分和资产生命周期测试结论。
